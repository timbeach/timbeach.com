timbeach.com.code-workspace
archive/arch-install.webm
.claude/
thailand_2026/photos/
tools/venv/
tools/models/
tools/__pycache__/
.superpowers/

# Generated social share pages (a/<slug>/index.html + og.png) — rebuilt by
# deploy.sh via tools/build_share_pages.py. Build artifact, like feed output.
/a/

# Generated full-text search index — rebuilt by deploy.sh via
# tools/build_search_index.py. Build artifact, like /a/.
/search-index.json

# Live site-counter state (created server-side, never committed)
counter-data/

