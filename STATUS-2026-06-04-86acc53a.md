# Session 2026-06-04 86acc53a

- Started: 21:22:47
- cwd: /home/trashh_panda/code/PROJECTS/VULTR_0/sites/timbeach.com
- session_id: 86acc53a-05f4-4038-aa32-96bbf006f9de

## Updates

### Social share previews (per-article OG images)

- 21:25 Problem: LinkedIn/etc. show the generic `zenshin-suru.jpg` for every shared article. Root cause: hash-routed SPA — `#/article/<slug>` is never sent to the server, and crawlers don't run JS, so they only ever read `index.html`'s static OG tags. No article had a `hero` either.
- Decision: generate crawlable per-article "share pages" at `a/<slug>/index.html` carrying that article's own OG/Twitter tags + a JS/`<meta refresh>` redirect into the SPA. Share the `/a/<slug>/` URL; humans land in the same reader view. Hash URL fundamentally cannot work for previews — explained to user, they accepted `/a/<slug>/`.
- og:image source: article `hero` if present (user is adding heroes), else an auto-generated branded 1200×630 card (night-theme palette, serif title) rendered with Pillow.
- New: `tools/build_share_pages.py` (driven by articles.json). Added Pillow to `tools/requirements.txt` + installed in venv. Wired into `deploy.sh` after feed gen. `a/` gitignored as a build artifact (rebuilt each deploy; not in `.deployignore` so it still rsyncs).
- Also (user request): added "⧉ Copy share link" button to the article header (`js/article.js` + `.share-link`/`.article-actions` in `css/site.css`) — copies `https://timbeach.com/a/<slug>/` with clipboard API + execCommand fallback.
- Verified: 20 share pages generated; curl shows per-article og:title/og:image + redirect; og.png 200s; article.js passes `node --check`. Card render looks clean/on-brand.
- NOTE for user: after deploy, re-scrape via LinkedIn Post Inspector. New `/a/` URLs are uncached so first share should be clean.

### Iteration 2 — hero from article images, then always-composite for sharpness

- 22:05 User asked: use each article's existing embedded image as the hero instead of the text card. Implemented auto-derive: generator scans article body for first `![](...)` (skips code fences), normalizes `../pix`→`pix`, uses it as hero; text card only for the 8 image-less articles. 12/20 now use a real image. Deployed; Post Inspector confirmed large card.
- 22:15 User: image renders but **blurry** (LinkedIn crop-resamples arbitrary-aspect sources + JPEG recompress, softens text screenshots; 3 images were also sub-1200px → small-thumbnail risk). Fix: generator now **always emits a 1200×630 og.png** — local hero images are contain-fit (Lanczos, whole image preserved — safe for diagrams) onto the night-theme canvas; text card fallback unchanged; remote `hero` URLs pass through. Every article → exact OG dimensions, no crawler resample. Verified all 20 og.png are 1200×630, claude-tips + agent-teams diagram render sharp. Deployed.
- Files this iteration: `tools/build_share_pages.py` (auto-derive + `generate_hero_card` composite), `CLAUDE.md` (share-pages section updated to always-composite).

### Iteration 3 — stale-cache fix + ship

- 22:34 User: inspector reverted to showing the original *text* card. Diagnosis: og:image URL was stable (`…/og.png`) across all iterations, but iter-1 had populated that exact URL into LinkedIn's image cache with the text card; LinkedIn caches images by URL and ignores changed bytes. Server byte-for-byte correct (confirmed via curl+md5).
- Fix: content-hash card filenames → `og-<hash>.png`; URL changes iff image changes. `deploy.sh` gained a `--delete`-scoped `a/` mirror so superseded hashes don't accumulate. Verified old `…/og.png` now 404s, new hashed URL 200s, page tag updated. User confirmed inspector now shows the sharp hero. 
- Shipped: committed `35b0799` ("Add per-article social share pages…") + pushed to main. Scoped to feature files only (build_share_pages.py, deploy.sh, js/article.js, css/site.css, requirements.txt, CLAUDE.md, .gitignore). Deliberately left `feed.xml` (timestamp-only churn, pre-dirty) and other sessions' untracked STATUS/IDEAS/.deployignore files untouched. No AI-attribution trailer (matches repo convention).
- Article write-up parked in circleback under BEACH/ for future-you.

